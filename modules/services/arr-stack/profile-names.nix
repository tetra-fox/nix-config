# the per-resolution quality profile names recyclarr manages, in preference order
# (first is the default the cleanup falls back to). single source of truth: recyclarr
# builds these profiles, and cleanup-profiles deletes any profile NOT in this list.
["best-2160p" "best-1080p" "best-720p" "best-sd"]
